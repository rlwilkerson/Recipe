### 2026-03-03T04:18:17Z: User directives — stack decisions

**By:** Rick Wilkerson (via Copilot)

**What:**
1. Use **.NET Aspire** for development orchestration (AppHost, service discovery, dashboard)
2. Use **PostgreSQL** as the database (not SQL Server / SQLite dev default)
3. Use **Playwright** for UI integration testing
4. Use **ASP.NET Core Identity** for authentication (confirmed/explicit)

**Why:** User request — captured for team memory
