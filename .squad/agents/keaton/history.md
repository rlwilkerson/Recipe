# Keaton — History

## Core Context

**Project:** Cookbook Web Application  
**User:** Rick Wilkerson  
**Stack:** ASP.NET Core Razor Pages, EF Core, ASP.NET Core Identity, HTMX, Bootstrap 5, PostgreSQL, .NET Aspire (orchestration), Playwright (UI testing)

**Domain Model Summary:**
- `ApplicationUser` (extends IdentityUser) — owns cookbooks and recipes
- `Cookbook` — Id (int), OwnerId, Name, Description, PublicId (unique), Slug, CreatedAt, UpdatedAt
- `Recipe` — Id (int), OwnerId, Title, Description, Ingredients, Instructions, PrepTime, CookTime, Servings, PublicId (unique), Slug, OriginalRecipeId (for clones), CreatedAt, UpdatedAt
- `CookbookRecipe` — join table (CookbookId, RecipeId, SortOrder), composite PK
- `Share` — grants Read or Update permission on a Cookbook or Recipe to another user

**Key Patterns:**
- URLs use `{publicId}/{slug}` — never internal int IDs
- publicId: random Base62, 8–12 chars, stable, unique per entity
- Slug: derived from name/title, lowercase with hyphens, NOT required to be unique
- Authorization: ownership OR Share record grants access
- Cookbook share implies read access to contained recipes

**URL Routes:**
- `/cookbooks` — list my cookbooks
- `/cookbooks/{publicId}/{slug}` — cookbook details
- `/recipes/{publicId}/{slug}` — recipe details

**Architecture: Vertical Slice + MediatR**
- Application logic lives in `Features/` folder, NOT in PageModels
- Each feature slice: `Features/{Domain}/{FeatureName}/{Command|Query}.cs`, `Handler.cs`, `Response.cs`
- Razor Pages inject `IMediator` and call `Send()` only — no business logic in .cshtml.cs
- Handlers are the unit-testable seam; testable without the web layer
- Future clients (API, CLI, mobile) dispatch the same MediatR requests
- Key slices to design: CreateCookbook, GetCookbook, ListCookbooks, AddRecipeToCookbook, ShareCookbook, CreateRecipe, GetRecipe, CloneRecipe, ShareRecipe

## Learnings

### 2026-03-03: Solution Scaffolding Complete

**Solution file:** `Recipe.slnx` (auto-generated by `dotnet sln` with .NET 10 SDK — the `.slnx` XML format replaces the older `.sln`)

**Projects:**
| Project | Type | Path |
|---|---|---|
| Recipe.Web | ASP.NET Core Razor Pages | `Recipe.Web/Recipe.Web.csproj` |
| Recipe.AppHost | .NET Aspire AppHost | `Recipe.AppHost/Recipe.AppHost.csproj` |
| Recipe.ServiceDefaults | Aspire Service Defaults | `Recipe.ServiceDefaults/Recipe.ServiceDefaults.csproj` |
| Recipe.Tests | xUnit unit tests | `Recipe.Tests/Recipe.Tests.csproj` |
| Recipe.Tests.Playwright | Playwright UI tests | `Recipe.Tests.Playwright/Recipe.Tests.Playwright.csproj` |

**Target framework:** net10.0 (SDK 10.0.200-preview.0 pinned in global.json)

**Key NuGet packages (Recipe.Web):**
- MediatR 12.4.1
- Microsoft.AspNetCore.Identity.EntityFrameworkCore 10.0.0-preview.3
- Microsoft.EntityFrameworkCore 10.0.0-preview.3
- Npgsql.EntityFrameworkCore.PostgreSQL 10.0.0-preview.3

**Key NuGet packages (Recipe.AppHost):**
- Aspire.Hosting.PostgreSQL 13.0.0

**Key NuGet packages (Recipe.Tests):**
- NSubstitute 5.3.0
- Microsoft.EntityFrameworkCore.InMemory 10.0.0-preview.3

**Key file paths:**
- `Recipe.Web/Program.cs` — full wiring: Aspire, Identity, EF, MediatR, services
- `Recipe.Web/Data/AppDbContext.cs` — IdentityDbContext<ApplicationUser> with DbSets
- `Recipe.Web/Models/` — ApplicationUser, Cookbook, Recipe, CookbookRecipe, Share (with ShareScope/SharePermission enums)
- `Recipe.Web/Features/` — vertical slice folders: Cookbooks/ (5 slices), Recipes/ (4 slices), Authorization/ (2 queries)
- `Recipe.Web/Services/` — IPublicIdService, ISlugService (stubs)
- `Recipe.Web/Pages/Shared/_Layout.cshtml` — Bootstrap 5.3.3 CDN + HTMX 2.0.4 CDN
- `Recipe.Web/Pages/Shared/_LoginPartial.cshtml` — Identity login/logout partial
- `Recipe.AppHost/AppHost.cs` — registers PostgreSQL + Recipe.Web

**Architectural decisions during scaffolding:**
- Used `AddIdentity<ApplicationUser, IdentityRole>()` instead of `AddDefaultIdentity()` to avoid requiring the Identity.UI NuGet package. Identity UI scaffolding can be added later.
- `Program.cs` includes `public partial class Program { }` to enable `WebApplicationFactory<Program>` in integration tests.
- Share model uses `ResourceId` (int) + `ShareScope` (enum) pattern to support sharing both Cookbooks and Recipes with a single table.
- The `.sln` file was not generated — .NET 10 SDK produces `.slnx` format. This is the new default.
